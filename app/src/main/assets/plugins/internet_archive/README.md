# Internet Archive plugin

This bundled plugin queries the public Internet Archive metadata endpoints and returns playable public-domain media files where available.

It is copied into the app-private `files/plugins/internet_archive` folder on first launch so it can be inspected, replaced, or used as a template for new plugins.
